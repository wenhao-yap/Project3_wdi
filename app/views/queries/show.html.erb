
<p>
  <strong>Searching for:</strong><%= @query.name %> 
</p>

<h1>Qoo10</h2>
<table>
  <tr>
    <th>No.</th>
    <th>title</th>
    <th>price</th>
  </tr>

   <% @parsedQoo10.each do |item| %>
  <tr>
  	<td><%= item.id %></td>
    <td><img src= <%= item.imageLink %>><span style="width:200px; height:100px; overflow:auto; display:inline-block;"><%= item.title %></span></td>
    <td><%= item.currPrice %></td>
  </tr>
  <% end %>

</table>

<h2>Shopee</h2>
<table>
  <tr>
    <th>No.</th>
    <th>title</th>
    <th>price</th>
  </tr>

   <% @parsedShopee.each do |item| %>
  <tr>
    <td><%= item.id %></td>
    <td><span style="width:200px; overflow:auto; display:inline-block;"><%= item.title %></span></td>
    <td><%= item.currPrice %></td>
  </tr>
  <% end %>

</table>
